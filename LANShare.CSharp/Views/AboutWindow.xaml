<Window x:Class="LANShare.CSharp.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About - LAN Network Share" Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E2E" Foreground="#CDD6F4">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Description -->
            <RowDefinition Height="Auto"/> <!-- Developer Credit Banner -->
            <RowDefinition Height="Auto"/> <!-- Close Button -->
        </Grid.RowDefinitions>

        <!-- Header with Logo -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,16">
            <Border Width="60" Height="60" CornerRadius="16" Background="#89B4FA" HorizontalAlignment="Center" Margin="0,0,0,12">
                <TextBlock Text="⚡" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#11111B"/>
            </Border>
            <TextBlock Text="{Binding AppName}" FontSize="24" FontWeight="Bold" Foreground="#89B4FA" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Version}" FontSize="13" Foreground="#A6ADC8" HorizontalAlignment="Center" Margin="0,3,0,0"/>
        </StackPanel>

        <!-- Description Box -->
        <Border Grid.Row="1" Background="#181825" CornerRadius="10" BorderBrush="#313244" BorderThickness="1" Padding="18" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="About Application" FontSize="14" FontWeight="Bold" Foreground="#89B4FA" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding Description}" FontSize="12.5" Foreground="#CDD6F4" TextWrapping="Wrap" LineHeight="19"/>
            </StackPanel>
        </Border>

        <!-- Prominent Developer Credit Banner -->
        <Border Grid.Row="2" Background="#313244" CornerRadius="10" BorderBrush="#F38BA8" BorderThickness="2" Padding="16,14" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="❤️ " FontSize="18" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <TextBlock Text="{Binding DeveloperCredits}" FontSize="15" FontWeight="Bold" Foreground="#F38BA8" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Close Button -->
        <Button Grid.Row="3" Content="Close" Command="{Binding CloseCommand}" IsDefault="True" Width="110" Height="36"
                HorizontalAlignment="Center" Background="#89B4FA" Foreground="#11111B" FontWeight="Bold" FontSize="13" BorderThickness="0" Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
